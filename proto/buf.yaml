version: v1beta1
build:
  roots:
    - .
lint:
  use:
    - DEFAULT
  ignore_only:
    ENUM_VALUE_PREFIX:
      - org/hypertrace/agent/config/v1/config.proto
    ENUM_ZERO_VALUE_SUFFIX:
      - org/hypertrace/agent/config/v1/config.proto
    PACKAGE_DIRECTORY_MATCH:
      - org/hypertrace/agent/config/v1/config.proto
breaking:
  use:
    - PACKAGE
    - WIRE_JSON
